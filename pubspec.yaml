name: reem_verse_rebuild
description: A clean rebuilt version of ReemVerse app 🧠
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.7.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  intl: ^0.19.0
  # UI
  cupertino_icons: ^1.0.2
  provider: ^6.1.1
  url_launcher: ^6.2.5
  flutter_secure_storage: ^9.2.4
  shared_preferences: ^2.2.2

  # Firebase
  firebase_core: ^3.13.0
  firebase_auth: ^5.5.3
  cloud_firestore: ^5.6.7
  firebase_storage: ^12.4.5

  # Media & Location
  just_audio: ^0.10.2
  audio_session: ^0.2.1
  image_picker: ^1.0.4
  permission_handler: ^12.0.0+1
  geolocator: ^14.0.0

  # Social login
  flutter_facebook_auth: ^7.1.2
  sign_in_with_apple: ^7.0.1

  # Cards UI / swipe
  swipe_cards: ^2.0.0+1

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/images/default_user.png
    - assets/videos/
    - assets/audios/
    - assets/fonts/
    - assets/icons/
